export const initialState = {
    isFetching: false,
    currentFact: null,
    categories: []
}

export const factReducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state
    }
}